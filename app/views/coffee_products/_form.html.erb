<%= form_for(coffee_product) do |f| %>
  <% if coffee_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coffee_product.errors.count, "error") %> prohibited this coffee_product from being saved:</h2>

      <ul>
      <% coffee_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :coffee_type_id %>
    <%= f.text_field :coffee_type_id %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
